# Core web / API
fastapi==0.141.1
uvicorn[standard]==0.52.3
jinja2==3.1.6
python-multipart==0.0.32

# Persistence
SQLAlchemy==2.0.52
alembic==1.19.1

# Data / config
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.2

# AI
anthropic==0.122.0

# Garmin (unofficial library)
garminconnect==0.3.2

# Strava (OAuth 2.0 + webhooks) — HTTP client
requests==2.34.2

# Object storage (Tigris / S3-compatible) for raw activity archive
boto3==1.43.71

# At-rest encryption of stored secrets (Strava OAuth tokens) - A10
cryptography==50.0.0

# Push notifications (FCM HTTP v1) - A3
httpx==0.28.1

# MCP server for the Claude custom connector (docs/MCP_CONNECTOR_ROADMAP.md).
# Only imported when MCP_PATH_TOKEN is set, so the app still boots without it.
mcp==2.0.0
